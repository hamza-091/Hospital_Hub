{
  "name": "hospital-hub",
  "version": "1.0.0",
  "description": "Hospital Management System — Full-stack monorepo with React frontend and Express backend",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "dev:frontend": "pnpm --filter @workspace/hms run dev",
    "dev:backend": "pnpm --filter @workspace/api-server run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./frontend\" --filter \"./backend\" --filter \"./scripts\" --if-present run typecheck",
    "seed": "pnpm --filter @workspace/scripts run seed"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "packageManager": "pnpm@10.33.1"
}
